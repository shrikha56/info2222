import React from "react";
import ReactDOM from "react-dom/client";
import StackdApp from "./StackdApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StackdApp />
  </React.StrictMode>,
);
